<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.CampusMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into campus (
            code, name, address, service_phone, status, delivery_fee, estimated_delivery_minutes,
            create_time, update_time, create_user, update_user
        ) values (
            #{code}, #{name}, #{address}, #{servicePhone}, #{status}, #{deliveryFee}, #{estimatedDeliveryMinutes},
            #{createTime}, #{updateTime}, #{createUser}, #{updateUser}
        )
    </insert>

    <update id="update">
        update campus
        <set>
            <if test="code != null">code = #{code},</if>
            <if test="name != null">name = #{name},</if>
            <if test="address != null">address = #{address},</if>
            <if test="servicePhone != null">service_phone = #{servicePhone},</if>
            <if test="status != null">status = #{status},</if>
            <if test="deliveryFee != null">delivery_fee = #{deliveryFee},</if>
            <if test="estimatedDeliveryMinutes != null">estimated_delivery_minutes = #{estimatedDeliveryMinutes},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="updateUser != null">update_user = #{updateUser}</if>
        </set>
        where id = #{id}
    </update>
</mapper>
