@charset "UTF-8";
/**
 * 这里是uni-app内置的常用样式变量
 *
 * uni-app 官方扩展插件及插件市场（https://ext.dcloud.net.cn）上很多三方插件均使用了这些样式变量
 * 如果你是插件开发者，建议你使用scss预处理，并在插件代码中直接使用这些变量（无需 import 这个文件），方便用户通过搭积木的方式开发整体风格一致的App
 *
 */
/**
 * 如果你是App开发者（插件使用者），你可以通过修改这些变量来定制自己的插件主题，实现自定义主题功能
 *
 * 如果你的项目同样使用了scss预处理，你也可以直接在你的 scss 代码中使用如下变量，同时无需 import 这个文件
 */
/* 颜色变量 */
/* 行为相关颜色 */
/* 文字基本颜色 */
/* 背景颜色 */
/* 边框颜色 */
/* 尺寸变量 */
/* 文字尺寸 */
/* 图片尺寸 */
/* Border Radius */
/* 水平间距 */
/* 垂直间距 */
/* 透明度 */
/* 文章场景相关 */
.order_page {
  min-height: 100vh;
  background: #f6f2ea;
}
.order_content {
  min-height: 100vh;
  padding: 180rpx 24rpx 220rpx;
  box-sizing: border-box;
}
.checkout_banner {
  padding: 30rpx 30rpx 34rpx;
  border-radius: 32rpx;
  background: radial-gradient(circle at top right, rgba(244, 162, 89, 0.26), transparent 32%), linear-gradient(145deg, #2f5d50 0%, #203d34 100%);
  box-shadow: 0 24rpx 60rpx rgba(24, 49, 41, 0.18);
  color: #ffffff;
}
.banner_eyebrow {
  font-size: 22rpx;
  line-height: 30rpx;
  color: rgba(244, 162, 89, 0.96);
  font-weight: 600;
}
.banner_title {
  display: block;
  margin-top: 14rpx;
  font-size: 40rpx;
  line-height: 52rpx;
  font-weight: 700;
}
.banner_desc {
  display: block;
  margin-top: 14rpx;
  font-size: 24rpx;
  line-height: 38rpx;
  color: rgba(255, 255, 255, 0.76);
}
.order_content_box {
  margin-top: 22rpx;
}
.footer_order_buttom {
  position: fixed;
  left: 24rpx;
  right: 24rpx;
  bottom: calc(28rpx + env(safe-area-inset-bottom));
  z-index: 30;
  display: flex;
  align-items: center;
  gap: 18rpx;
  padding: 16rpx;
  border-radius: 32rpx;
  background: rgba(21, 36, 31, 0.94);
  box-shadow: 0 26rpx 60rpx rgba(20, 36, 31, 0.24);
}
.pay_summary {
  min-width: 0;
  flex: 1;
  display: flex;
  flex-direction: column;
}
.pay_label {
  font-size: 24rpx;
  line-height: 34rpx;
  color: rgba(255, 255, 255, 0.62);
}
.pay_desc {
  margin-top: 8rpx;
  font-size: 22rpx;
  line-height: 32rpx;
  color: rgba(255, 255, 255, 0.72);
}
.pay_price {
  flex-shrink: 0;
  font-size: 40rpx;
  line-height: 52rpx;
  color: #ffffff;
  font-weight: 700;
}
.pay_price .ico {
  font-size: 24rpx;
}
.order_but {
  flex-shrink: 0;
  min-width: 204rpx;
  height: 88rpx;
  padding: 0 28rpx;
  border-radius: 999rpx;
  background: #f4a259;
  color: #ffffff;
  text-align: center;
  line-height: 88rpx;
  font-size: 28rpx;
  font-weight: 700;
}
.order_but--disabled {
  background: rgba(255, 255, 255, 0.18);
  color: rgba(255, 255, 255, 0.6);
}
 .uni-nav-bar .uni-nav-bar-text {
  font-size: 30rpx !important;
  font-weight: 700 !important;
}
 .uni-nav-bar .uni-nav-bar-btn-icon-left {
  color: #ffffff !important;
}
